# 実装したEntity:

- Prefecture
- WeatherRecord
- DailyForecast

# 学んだJPAアノテーション:

- @entity, @table, @id, @GeneratedValue
- @column, @manytoone, @onetomany
- @PrePersist, @PreUpdate

# 困難だった点:
複数のEntityクラスを実装して、全体を通して何をしているのかを把握するのが難しく感じた。
また、ファイルの配置場所を誤りエラーを起こしていることに気がつくまでに時間を要してしまい、
解決までに余分な時間をかけてしまった。

# 明日への引き継ぎ:
Repository層の実装準備

